services:
  glance:
    image: glanceapp/glance:v0.8.6
    container_name: glance
    restart: unless-stopped
    environment:
      TZ: Europe/Helsinki
    volumes:
      - ./config:/app/config:ro
    ports:
      - "8090:8080"
